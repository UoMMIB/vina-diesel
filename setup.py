from setuptools import setup, find_packages

setup(name='vdsl',
        version='0.0', 
        description = 'autodock vina interface',
        author='james engleback',
        packages = find_packages())
